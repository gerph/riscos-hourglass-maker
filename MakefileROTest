# Build the test code

VPATH = .. @

ifeq (${BUILD64},1)
OASM = o.asm64
else
OASM = o.asm
endif

COMPONENT  = hourglass_test
TYPE       = aif
CDEFINES   =
INCLUDES   =
LIBS       = ${CLIB}
OBJS       = \
             ${OASM} \
             o.hourglass_test

include LibraryCommand
